# Historical Authorization Pointer — Mission Control Stages 3–5

The original 2026-08-09 tranche-authorization document is preserved unchanged at:
`docs/history/legacy/MISSION_CONTROL_BUILD_TRANCHE_2026-08-09.md`.

The authorization remains active, but its **current operational expression** is now:
- `docs/STATE.md`
- `docs/ROADMAP.md`
- `/qamc-build` in `.claude/skills/qamc-build/`

This avoids making Claude reconcile a historical workflow document with the native Claude Code operating model on every build.
