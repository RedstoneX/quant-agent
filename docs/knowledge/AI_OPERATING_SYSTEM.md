# Deprecated: AI Operating System

The document that previously lived here has been preserved at
`docs/history/legacy/AI_OPERATING_SYSTEM_2026-08-09.md`.

QAMC now uses Claude Code's native project model:
- root `CLAUDE.md` for small always-on instructions;
- `.claude/rules/` for path-scoped constraints;
- `.claude/skills/` for on-demand workflows;
- `.claude/agents/` for isolated specialists/reviewers;
- `.claude/settings.json` and hooks for deterministic guardrails.

Current state is `docs/STATE.md`. Do not use the legacy AI Operating System as current instruction.
