# Deliberately paused systemd units.
#
# `scripts/check_unit_drift.py` reads this file. A unit listed here that is
# installed-but-not-enabled is a deliberate pause, reported separately and
# NOT counted as drift. A listed unit that IS enabled means the box and this
# list disagree — that is reported as drift, on the theory that someone
# re-enabled it without updating the list. A listed unit that does not exist
# in this directory is an error in the list itself and is reported as one.
#
# Add an entry when a unit is paused; remove it in the same change that
# re-enables the unit (`systemctl --user enable --now <unit>`) — see the
# restart procedure in README.md.
#
# Each entry:
#   unit:   exact filename in scripts/systemd/
#   since:  date paused (YYYY-MM-DD)
#   reason: one line, why

paused_units:
  - unit: quant-agent-morning.timer
    since: "2026-09-14"
    reason: "trading desk paused pending PM model exam; owner approved reboot-proof pause 2026-09-14"
  - unit: quant-agent-midday.timer
    since: "2026-09-14"
    reason: "trading desk paused pending PM model exam; owner approved reboot-proof pause 2026-09-14"
  - unit: quant-agent-close.timer
    since: "2026-09-14"
    reason: "trading desk paused pending PM model exam; owner approved reboot-proof pause 2026-09-14"
  - unit: quant-agent-intra_check.timer
    since: "2026-09-14"
    reason: "trading desk paused pending PM model exam; owner approved reboot-proof pause 2026-09-14"
  - unit: quant-agent-evening.timer
    since: "2026-09-14"
    reason: "trading desk paused pending PM model exam; owner approved reboot-proof pause 2026-09-14"
  - unit: quant-agent-earnings_preprocess.timer
    since: "2026-09-14"
    reason: "trading desk paused pending PM model exam; owner approved reboot-proof pause 2026-09-14"
