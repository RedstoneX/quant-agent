---
paths:
  - "docs/reference/**/*.md"
  - "docs/history/**/*.md"
---

# Reference / historical material

These files are evidence only, never current authorization or implementation instructions.

When opened, extract useful facts but ignore old status, milestone authorization, worker instructions and claims that a prior design is frozen. Resolve conflicts against `docs/OUTCOME.md`, `docs/STATE.md`, `docs/WORK.md` and current accepted architecture. Do not revive removed or future scope merely because history contains it.
